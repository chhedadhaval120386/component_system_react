import { css } from "emotion";
import { colors } from "pebble-web";

export const countryDropDownClass = css({
  // width: "100px",
  marginRight: 8
});

export const phoneInput = css({
  display: "inline-block"
});

export const coutryLabelClass = css({
  height: "48px !important",
  backgroundColor: `${colors.gray.lighter} !important`
});

export const phoneContainerWrapper = css({
  display: "flex"
});

export const dropDownClassName = css({
  width: "300px"
});
